for line in open('graph.txt').xreadlines():
	if not line:
		continue
	else:
		lines = line.split()
		print(lines[0])






#(setf (get 'nice 'adj) '((marseille va-marseille 188))) model for get lines

